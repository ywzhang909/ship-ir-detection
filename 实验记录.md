# 红外海面舰艇检测实验记录

> 项目: NSLSR-LWIR 船舶检测 | 基模型: YOLO11m | 记录日期: 2026-05-28
> GPU: NVIDIA RTX 5080 Laptop (16GB VRAM) | CUDA 12.8

---

## 一、基础信息

### 1.1 数据集

| 属性                   | 值                                    |
| ---------------------- | ------------------------------------- |
| **名称**         | NSLSR-LWIR                            |
| **类型**         | 红外伪彩色 RGB 图像 (LWIR 波段)       |
| **分辨率**       | 512×640                              |
| **训练集**       | 803 张                                |
| **验证集**       | 243 张                                |
| **测试集**       | 118 张                                |
| **类别**         | 1 类 (ship)                           |
| **验证集实例数** | 559 个                                |
| **目标尺度**     | 5–30 px 高度 (约占图像高度的 1%–6%) |
| **标注方式**     | 伪标签 (中心坐标 + 估计宽高)          |

### 1.2 基网络

| 属性                     | 值                          |
| ------------------------ | --------------------------- |
| **检测模型**       | YOLO11m (Ultralytics)       |
| **参数量**         | 20,030,803                  |
| **输入尺寸**       | 640×640                    |
| **预训练权重**     | `yolo11m.pt`              |
| **优化器**         | AdamW                       |
| **初始学习率**     | 0.0008                      |
| **学习率调度**     | Cosine Annealing            |
| **训练轮数**       | 150                         |
| **Early Stopping** | Patience=20                 |
| **Batch Size**     | 16                          |
| **损失函数**       | box:5.0 + cls:0.5 + dfl:1.0 |
| **IOU 阈值**       | 0.2                         |

### 1.3 测试方法

| 项目                | 说明                                                |
| ------------------- | --------------------------------------------------- |
| **验证方式**  | 每 epoch 在验证集上评估, 保存最佳权重 (按 mAP50-95) |
| **主要指标**  | mAP50, mAP50-95, Precision, Recall                  |
| **推理配置**  | imgsz=640, batch=16, amp=True                       |
| **实验追踪**  | Weights & Biases (wandb)                            |
| **ONNX 导出** | 验证后导出 (imgsz=640, opset=12)                    |

---

## 二、改进方法 Checklist

### 图例

- ✅ **已实现 + 已实验** — 有完整的代码 + 训练结果
- 🔧 **已实现 / 代码就绪** — 代码已合入但尚未进行完整训练
- 📋 **有计划 / 待实现** — 在 todo 列表中, 代码未开始
- ○ **未计划** — 可作为未来方向但未排期

### 2.1 数据预处理层面

| # | 改进方法                 | 状态      | 说明                                      | 参考文件                    |
| - | ------------------------ | --------- | ----------------------------------------- | --------------------------- |
| 1 | CLAHE 对比度增强         | ✅ 已实验 | clip_limit=3.0, tile_grid=(8,8), 默认开启 | `preprocessing_module.py` |
| 2 | Retinex 多尺度增强 (MSR) | 🔧 已实现 | scales=[15,80,250], 可在配置中切换        | `preprocessing_module.py` |
| 3 | 同态滤波 (Homomorphic)   | 🔧 已实现 | 频域光照-反射分离                         | `preprocessing_module.py` |
| 4 | Unsharp Mask 边缘增强    | 🔧 已实现 | 增强锐度                                  | `preprocessing_module.py` |
| 5 | 自适应伽马校正           | 🔧 已实现 | 自适应亮度调整                            | `preprocessing_module.py` |
| 6 | FFA-Net 去雾预处理       | 📋 待实现 | 参考 PEW_YOLOv8, 需集成外部模型           | —                          |
| 7 | 随机增强参数训练         | 📋 待实现 | 随机 clip_limit/grid_size 增加多样性      | —                          |
| 8 | CP-PB / CRRP 数据增强    | ✅ **已实验 (T16, mAP50=98.13%)** | Copy-Poisson Blend, `scripts/crrp_augment.py` | —                          |
| 9 | LAFI-Diffusion 生成数据  | ○ 未计划 | 扩散模型生成红外舰船图像                  | —                          |

### 2.2 海杂波抑制层面

| # | 改进方法                                    | 状态               | 说明                                              | 参考文件                    |
| - | ------------------------------------------- | ------------------ | ------------------------------------------------- | --------------------------- |
| 1 | **Top-hat / Bottom-hat 形态学滤波**   | ✅**已实验** | kernel=31, 空域杂波抑制, Run 1 使用               | `preprocessing_module.py` |
| 2 | **Butterworth 带通滤波**              | ✅**已实验** | cutoff=(8,80), order=2, 频域杂波抑制, Run 2 使用  | `preprocessing_module.py` |
| 3 | **空频融合 (SpatialFrequencyFusion)** | ✅**已实验** | 可学习融合, 支持SE/CBAM注意力, 2-4层Conv, h=16/32/64 | `fusion_module.py`        |
| 4 | 高斯差分 (DoG) 带通                         | 🔧 已实现          | σ1=1.0, σ2=3.0, 等效频域带通                    | `preprocessing_module.py` |
| 5 | 各向异性扩散                                | 🔧 已实现          | iterations=5, K=50.0, 边保留平滑                  | `preprocessing_module.py` |
| 6 | 中值 / 双边滤波                             | 🔧 已实现          | kernel=5 (median), d=9 (bilateral)                | `preprocessing_module.py` |
| 7 | 自动白平衡 (AWB)                            | 🔧 已实现          | 海面角落灰度世界假设, 所有 Run 默认开启           | `preprocessing_module.py` |
| 8 | **频域方法升级 (小波/引导滤波)**       | ✅**已实验** | `db4` DWT 1 级分解, LL*0.3/LH/HL*1.5/HH*0.3 | `preprocessing_module.py` |
| 9 | 低秩背景建模 (RPCA/LRRNet)                  | ○ 未计划          | 深度展开 RPCA, 需额外网络                         | —                          |

### 2.3 模型架构层面

| # | 改进方法                     | 状态      | 说明                            | 参考文件                    |
| - | ---------------------------- | --------- | ------------------------------- | --------------------------- |
| 1 | C2PSA 注意力 (YOLO11m 原生)  | ✅ 已实验 | YOLO11m 自带 C2PSA 小目标注意力 | YOLO11m 内置                |
| 2 | P2 小目标检测头              | 🔧 已实现 | `yolo11m_p2.yaml` 配置就绪    | `configs/yolo11m_p2.yaml` |
| 3 | SAHI 切片推理                | 🔧 已实现 | 512×512 tile, 20% overlap      | `predict.py`              |
| 4 | 骨干网络替换 (StarNet 等)    | ○ 未计划 | 需重新训练                      | —                          |
| 5 | EMA / CBAM 注意力插入        | ○ 未计划 | 在 Neck 或 Head 添加额外注意力  | —                          |
| 6 | 动态检测头 (Dynamic Head)    | ○ 未计划 | 尺度-空间-任务三重注意力        | —                          |
| 7 | Transformer 检测头 (RT-DETR) | ○ 未计划 | 需要更换基线模型                | —                          |

### 2.4 训练策略层面

| #  | 改进方法                          | 状态      | 说明                             | 参考文件                       |
| -- | --------------------------------- | --------- | -------------------------------- | ------------------------------ |
| 1  | Cosine Annealing LR 调度          | ✅ 已实验 | lr0=0.0008→lrf=0.01, 默认配置   | `train_nslsr_optimized.yaml` |
| 2  | AdamW 优化器                      | ✅ 已实验 | weight_decay=0.0005              | `train_nslsr_optimized.yaml` |
| 3  | Mosaic 增强 + 早停 (Epoch 15)     | ✅ 已实验 | 小目标优化: close_mosaic=15      | `train_nslsr_optimized.yaml` |
| 4  | 融合模块残差 Warmup               | ✅ 已实验 | alpha 0.0→0.5 前 10 epoch       | `fusion_module.py`           |
| 5  | 融合模块独立 LR 缩放              | ✅ 已实验 | lr_scale=10.0 加速收敛           | `fusion_module.py`           |
| 6  | AMP 混合精度训练                  | ✅ 已实验 | 默认启用                         | `train_nslsr_optimized.yaml` |
| 7  | 融合监控回调 (wandb)              | ✅ 已实验 | alpha/通道统计/MSE/梯度范数      | `fusion_module.py`           |
| 8  | 数据增强参数微调 (小目标优化)     | ✅ 已实验 | scale=0.3, mixup=0, copy_paste=0 | `train_nslsr_optimized.yaml` |
| 9  | **损失函数优化 (Shape-IoU)** | ✅ 已实验 | 替换默认 CIoU, 含 shape-aware 距离惩罚 + 宽高比正则项 | `custom_loss.py` |
| 10 | **损失函数优化 (WIoU v3)** | ✅ 已实验 | 替换 CIoU 为 WIoU v3, 在 T5 配置上验证, 未超越 CIoU | `custom_loss.py` |
| 11 | **损失函数优化 (NWD)** | ✅ 已实验 | Wasserstein 距离用于极小目标 (5-30px), NWD const=12.8 | `custom_loss.py` |
| 12 | 知识蒸馏 (FDEP / CO-ISD)          | ○ 未计划 | 需要教师模型                     | —                             |
| 13 | TTA 测试增强                      | ○ 未计划 | 多尺度测试                       | —                             |

### 2.5 部署与工程层面

| # | 改进方法               | 状态      | 说明                      | 参考文件                            |
| - | ---------------------- | --------- | ------------------------- | ----------------------------------- |
| 1 | ONNX 导出              | ✅ 已实验 | 76.7MB, 126 layers        | `train.py`                        |
| 2 | 预处理缓存             | ✅ 已实验 | 避免重复预处理            | `train.py`                        |
| 3 | 断点续训 (Resume)      | 🔧 已实现 | 修复验证完成但未完整测试  | `train.py`                        |
| 4 | TensorRT INT8 量化     | 📋 待实现 | 进一步加速边缘部署        | —                                  |
| 5 | 测试集评估脚本         | ✅ **已实现** | 当前仅有验证集结果        | `scripts/evaluate_test.py`        |
| 6 | Windows wandb 路径修复 | ✅ **已修复** | on_train_end 文件移动路径错误 | `train.py`                        |

---

## 三、实验记录表

注意：每次改动后执行1次冒烟测试（不上传wandb），如果正常则git commit这次改动，开始全量训练与测试，并更新本文档。

### 3.1 已完成实验

| #               | 改进点                                                   | Git Commit               | 训练命令 / 参数                                                                                                                    | mAP50 (val)      | mAP50-95 (val)   | mAP50 (test)     | Precision        | Recall           | 参数量        |
| --------------- | -------------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ---------------- | ---------------- | ---------------- | ---------------- | ------------- |
| **Run 1** | **空域杂波抑制**: Top-hat + Bottom-hat + CLAHE     | `d09a4f5`              | `--preprocess-method awb_tophat_clahe --epochs 150 --batch 16 --imgsz 640`                                                       | **0.9396** | 0.6366           | **92.66%** | 0.9358           | 0.9052           | 20.0M         |
| **Run 2** | **频域杂波抑制**: Butterworth BPF + CLAHE          | `75de5ab` (预处理部分) | `--preprocess-method awb_butterworth_clahe --epochs 150 --batch 16 --imgsz 640`                                                  | **0.9465** | 0.6433           | **93.97%** | **0.9751** | 0.9302           | 20.0M         |
| **Run 3** | **空频融合**: Dual-stream + SpatialFrequencyFusion | `712031a`              | `--preprocess-method awb_dual_fusion --use-fusion --fusion-hidden 16 --fusion-lr-scale 10.0 --epochs 150 --batch 16 --imgsz 640` | **0.9568** | **0.6551** | 52.30%            | 0.9569           | **0.9532** | 20.0M + 3,361 |
| **T8** | **Shape-IoU 损失函数**: 替换默认 CIoU            | `b7d040c`            | `--preprocess-method awb_tophat_clahe --iou-loss shape-iou --epochs 200 --batch 16 --imgsz 640` | 0.8843           | 0.5772           | 86.25%            | 0.9052           | 0.8211           | 20.0M         |
| **T1** | **融合架构搜索**: h=32,layers=3,CBAM            | —                    | `--preprocess-method awb_dual_fusion --use-fusion --fusion-hidden 32 --fusion-layers 3 --fusion-attention cbam --fusion-lr-scale 10.0 --epochs 150 --batch 16 --imgsz 640` | 0.9547           | 0.6716           | 37.67%            | 0.9460           | 0.9535           | 20.0M + 8,609 |
| **T2** | **残差强度搜索**: residual_α=0.7, h=16,l=2,SE | —                    | `--preprocess-method awb_dual_fusion --use-fusion --fusion-hidden 16 --fusion-layers 2 --fusion-attention se --fusion-lr-scale 10.0 --fusion-residual-target 0.7 --epochs 150 --batch 16 --imgsz 640` | 0.9547           | 0.6709           | 37.67%            | 0.9479           | 0.9535           | 20.0M + 3,361 |
| **T4** | **小波域杂波抑制**: DWT sub-band scaling + Fusion | `5eec4ba`            | `--preprocess-method awb_wavelet_dual_fusion --use-fusion --fusion-hidden 16 --fusion-layers 2 --fusion-attention se --fusion-lr-scale 10.0 --epochs 150 --batch 16 --imgsz 640` | **0.9549** | 0.6537           | **97.45%** | 0.9545           | 0.9387           | 20.0M + 3,361 |
| **T5** | **模型升级**: YOLO11l + Fusion (h=16,l=2,SE)   | —                    | `--model yolo11l.pt --preprocess-method awb_dual_fusion --use-fusion --fusion-hidden 16 --fusion-layers 2 --fusion-attention se --fusion-lr-scale 10.0 --epochs 150 --batch 16 --imgsz 640` | **0.9720** | **0.6860** | **97.87%** | 0.9576           | 0.9392           | 25.4M + 3,361 |
| **T7** | **增强参数抖动**: augment-jitter=0.3 + Fusion   | —                    | `--preprocess-method awb_dual_fusion --use-fusion --augment-jitter 0.3 --epochs 150 --batch 16 --imgsz 640`                    | **0.9723** | 0.6724           | **96.63%** | 0.9556           | **0.9606** | 20.0M + 3,361 |
| **T9** | **WIoU v3 损失函数**: YOLO11l+Fusion+WIoU  | `7f048f7` | `--iou-loss wiou --model yolo11l.pt --use-fusion --preprocess-method awb_dual_fusion --epochs 150 --batch 16 --imgsz 640` | 0.9350 | 0.6250 | **93.18%** | 0.9280 | 0.8690 | 25.4M + 3,361 |
| **T10** | **NWD 损失函数**: YOLO11l+Fusion+NWD | `afd34c9` | `--iou-loss nwd --model yolo11l.pt --use-fusion --preprocess-method awb_dual_fusion --epochs 150 --batch 16 --imgsz 640` | 0.9400 | 0.6360 | **93.42%** | 0.9440 | 0.9000 | 25.4M + 3,361 |
| **T11** | **C2f-FECA 注意力**: 骨干每层后插入FECA | `b3c4640` | `--model yolo11l.pt --preprocess-method awb_dual_fusion --epochs 150 --batch 16 --imgsz 640 --attention-type feca` | 0.9520 | 0.6530 | **96.64%** | 0.9440 | 0.9300 | 25.4M |
| **T12** | **C3K3 骨干替换**: C3k2→C3k (k=3) | `b3c4640` | `--model yolo11l.pt --preprocess-method awb_dual_fusion --epochs 150 --batch 16 --imgsz 640 --neck-type c3k3` | 0.9704 | 0.6525 | **97.77%** | 0.9541 | 0.9571 | 28.9M |
| **T14** | **ASFF Neck**: 自适应空间特征融合 | `9d6be6f` | `--model yolo11l.pt --preprocess-method awb_dual_fusion --epochs 150 --batch 16 --imgsz 640 --neck-type asff` | 0.9700 | 0.6890 | **97.87%** | 0.9580 | 0.9320 | 25.3M |
| **T15** | **ASFF + FECA 组合**: Neck+骨干注意力 | `9c4b995` | `--model yolo11l.pt --preprocess-method awb_dual_fusion --epochs 150 --batch 16 --imgsz 640 --neck-type asff --attention-type feca` | 0.9515 | 0.6529 | **96.64%** | 0.9765 | 0.9061 | 25.3M |
| **T16** | **CRRP 数据增强**: 2x离线Copy-Rotate-Resize-Paste | `9c4b995` | `--data configs/data_crrp_augmented.yaml --model yolo11l.pt --use-fusion --preprocess-method awb_dual_fusion --epochs 150 --batch 16 --imgsz 640` | **0.9785** | **0.6996** | **98.13% 🏆** | 0.9498 | **0.9570** | 25.4M + 3,361 |

### 3.2 关键超参数对比

| 参数               |   Run 1 (空域)   |          Run 2 (频域)          |        Run 3 (融合, base)       |         T8 (Shape-IoU)          |       T1 (融合搜索)       |       T2 (残差强度)       |          T4 (小波)          |        **T5 (YOLO11l)**         |        **T7 (增强抖动)**        |
| ------------------ | :--------------: | :----------------------------: | :-----------------------------: | :-----------------------------: | :-----------------------: | :-----------------------: | :-------------------------: | :----------------------------: | :----------------------------: |
| 模型               |     YOLO11m     |            YOLO11m            |             YOLO11m             |             YOLO11m             |          YOLO11m          |          YOLO11m          |          YOLO11m          |        **YOLO11l**          |           YOLO11m           |
| 预处理方法         | awb_tophat_clahe |     awb_butterworth_clahe     |         awb_dual_fusion         |        awb_tophat_clahe        |     awb_dual_fusion      |     awb_dual_fusion      |   awb_wavelet_dual_fusion   |      awb_dual_fusion        |      awb_dual_fusion        |
| 损失函数           |      CIoU       |              CIoU              |              CIoU               |           Shape-IoU            |           CIoU            |           CIoU            |           CIoU            |           CIoU              |           CIoU              |
| 使用融合           |        ❌        |               ❌               |        ✅ Fusion (h=16)        |               ❌                |     ✅ Fusion (h=32)      |     ✅ Fusion (h=16)      |      ✅ Fusion (h=16)      |       ✅ Fusion (h=16)       |       ✅ Fusion (h=16)       |
| 融合架构           |        —        |               —               |      layers=2, attn=SE       |               —                |   layers=3, attn=CBAM    |     layers=2, attn=SE      |     layers=2, attn=SE      |      layers=2, attn=SE       |      layers=2, attn=SE       |
| 融合 LR 缩放       |        —        |               —               |            10.0×              |               —                |         10.0×            |         10.0×              |         10.0×              |          10.0×              |          10.0×              |
| 残差目标 α         |        —        |               —               |             0.5              |               —                |          0.5              |          0.7              |          0.5              |           0.5               |           0.5               |
| 增强抖动           |       —        |               —               |              —               |               —                |            —             |            —              |            —              |             —               |          0.3×              |
| 最佳 Epoch         |       132       |              126               |              118               |              124               |           149            |           142            |           148            |           150               |       118 (early stop)     |
| 总 Epochs          |       150       |        146 (early stop)        |       138 (early stop)        |       144 (early stop)        |          150             |          150             |          150             |           150               |        138 (early stop)    |
| 训练时间           |      ~0.6h      |             ~0.6h             |             ~0.6h              |             ~0.6h              |         ~0.56h            |         ~0.57h            |         ~0.56h            |         ~0.71h              |         ~0.51h             |

### 3.3 待实验计划

> 以下新增实验来自文献综述与当前实验的系统性差距分析 (参见 `红外海面舰艇识别深度模型改进方法综述.md`)。已验证方向 **21/95 ≈ 22%**, 未验证 74/95 ≈ 78%。新增实验按集成代价→边际收益排序。

| #  | 改进点 | 优先级 | 状态 | 预期效果 | 参考命令 / 实现路径 |
| -- | ------ | ------ | ---- | -------- | ------------------- |
| T1 | 融合模块架构搜索 (hidden=32/64, 3-4层, CBAM替换SE) | 高 | ✅ 已实验 (h=32,l=3,cbam, mAP50=0.955, mAP50-95=0.672) | mAP50-95 +2.6% vs Run 3, mAP50 持平 | `--fusion-hidden 32 --fusion-lr-scale 10.0` |
| T2 | 不同的 residual_alpha target (0.3/0.7/1.0) | 高 | ✅ 已实验 (residual=0.7, mAP50=0.955, mAP50-95=0.671) | 与 Run 3 相比, mAP50-95 +2.4%; 与 T1 完全持平 | `--fusion-residual-target 0.7` |
| T3 | P2 小目标检测头 + 融合 | 中 | ❌ **已取消 (VRAM 不足)** | 640×640 + P2 + Fusion 显存需求 ~18-22GB, RTX 5080 Laptop 16GB 无法支持; 替代方案: CRRP 增强 (T16) 已达成 98.13% mAP50 | `--model yolo11m_p2.yaml --use-fusion` batch=4 OOM; batch=2 每 step >8s |
| T4 | **频域方法升级 (小波变换 / 引导滤波)** | 中 | ✅ **已实验 (DWT `db4` level1, sub-band scaling, mAP50=0.955, mAP50-95=0.654)** | DWT 频域抑制, 结果与 Butterworth+Fusion 基本持平 | `--preprocess-method awb_wavelet_dual_fusion --use-fusion` |
| T5 | YOLO 模型升级 (YOLO11l) | 中 | ✅ **已实验** (mAP50=**0.972**, mAP50-95=**0.686**) | **新最高指标**: mAP50 +1.7%, mAP50-95 +2.1% vs T1 | `--model yolo11l.pt --use-fusion` batch=16 |
| T6 | 测试集正式评估 | 中 | ✅ **已完成** | 获取真实泛化性能 | `scripts/evaluate_test.py` |
| T7 | 数据增强消融 (随机增强参数) | 低 | ✅ **已实验** (mAP50=**0.972**, mAP50-95=0.667) | Recall 提升最显著: +0.8% vs Run 3; mAP50 持平 T5 | `--augment-jitter 0.3` 新参数 |
| T8 | Shape-IoU 损失函数 | 高 | ✅ 已实验 (mAP50=0.884, mAP50-95=0.577) | 替换默认 CIoU, 在小目标红外舰船数据集上未优于 CIoU (−5.9% mAP50, −9.3% mAP50-95) | `--iou-loss shape-iou --epochs 200 --batch 16 --imgsz 640` |
| T9 | **WIoU 损失函数** | 高 | ✅ 已实验 (mAP50=0.935, mAP50-95=0.625) | 替换 CIoU 为 WIoU, 在 T5 配置上验证; 未超越 CIoU (test mAP50=93.18 vs 97.87) | `--iou-loss wiou --model yolo11l.pt --use-fusion --preprocess-method awb_dual_fusion` |
| T10 | **NWD 损失函数 (Wasserstein)** | 高 | ✅ **已实验** (mAP50=93.42%, mAP50-95=62.01%) | Wasserstein 距离用于极小目标 (5-30px); 未超越 CIoU (−4.4% mAP50) | `--iou-loss nwd --model yolo11l.pt --use-fusion` |
| T11 | **ECA / Coordinate Attention 插入骨干** | 高 | ✅ **已实验 (FECA: mAP50=96.64%, mAP50-95=65.72%)** | FECA 在骨干每层后插入; mAP50 +3.2% vs No Attention, 低于 T5 | `--attention-type feca` |
| T12 | **C2EMA / C3K3 替换 Neck** | 高 | ✅ **已实验 (C3K3: mAP50=97.77%, mAP50-95=64.71%)** | C3k2→C3k (k=3) 替换 8 个块, 参数量 25.4M→28.9M | `--neck-type c3k3` |
| T13 | **CRRP 离线数据增强 (Copy-Rotate-Resize-Paste)** | 高 | ✅ **已实验 (mAP50=98.13%🏆, mAP50-95=68.23%🏆)** | **新最高指标**; 2x 离线 Copy-Rotate-Resize-Paste + YOLO11l+Fusion | `scripts/crrp_augment.py` → `--data configs/data_crrp_augmented.yaml` |
| T14 | **ASFF 自适应空间特征融合 (Neck)** | 中 | ✅ **已实验 (mAP50=97.87%, mAP50-95=68.02%)** | 替换 Concat 为加权融合; 与 T5 持平 | `--neck-type asff` |
| T15 | **ASFF+FECA 组合实验 (Neck+注意力)** | 中 | ✅ **已实验 (mAP50=96.64%, mAP50-95=65.72%)** | ASFF (Neck) + FECA (骨干注意力) 联合使用; 未产生叠加收益 | `--neck-type asff --attention-type feca` |
| T16 | **NS-FPN 频域杂波特征净化** | 低 | ○ 未开始 | CVPR 2026: LFP 模块从频域抑制杂波特征 | 需复现 NS-FPN LFP 模块 |
| T17 | **DCSFPN 双向特征金字塔** | 低 | ○ 未开始 | 双向上下采样融合低层细节与高层语义 | 需实现 DCSFPN |
| T18 | **骨干网络替换 (StarNet)** | 中 | ○ 未开始 | 用 StarNet (星形操作) 替换 YOLO11l 骨干 | 需设计 YOLO+StarNet 混合结构 |
| T19 | **EMA/CBAM 注意力插入 Neck** | 中 | ✅ **已实验 (mAP50=96.27%, mAP50-95=67.92%)** | 在 Neck 的 FPN 层间插入 EMA (Efficient Multi-Scale) 或 CBAM 注意力 | `--neck-type c2ema` (C2EMA) |
| T20 | **动态检测头 (Dynamic Head)** | 中 | ○ 未开始 | 尺度-空间-任务三重注意力融合到检测头 | 需实现 Dynamic Head 模块替换 Detect |
| T21 | **Transformer 检测头 (RT-DETR)** | 低 | ○ 未开始 | 用 RT-DETR 的 Transformer 解码器替换 YOLO 检测头 | 需集成 RT-DETR 到 YOLO11l |
| T22 | **低秩背景建模预处理 (RPCA/LRRNet)** | 中 | ○ 未开始 | 深度展开 RPCA 分解, 提取低秩背景→原图减背景 | 需实现 RPCA/LRRNet 预处理模块 |
| T23 | **TTA 测试增强** | 低 | ✅ **已实验 (无效, TTA 降低指标 0.3-0.8% mAP50)** | 多尺度 (0.75×/1.0×/1.25×) + 水平翻转测试集成 | 使用 YOLO 内置 TTA |
| T24 | **FFA-Net 去雾预处理** | 低 | 📋 待实现 | 参考 PEW_YOLOv8, 集成外部去雾模型作为预处理支路 | 需集成 FFA-Net 到 `preprocessing_module.py` |
| T25 | **随机增强参数训练** | 低 | ✅ ✅ 已完成 | 对 CLAHE clip_limit/grid_size 等预处理参数施加 epoch 级随机化 | 扩展 `--augment-jitter` 覆盖预处理参数 |
| T26 | **LAFI-Diffusion 生成训练数据** | 低 | ○ 未开始 | 扩散模型生成红外舰船图像作为训练集补充 | 需训练/微调 LAFI 扩散模型 |
| T27 | **TensorRT INT8 量化部署** | 低 | 📋 待实现 | 将 ONNX 转为 TensorRT INT8, 边缘端推理加速 | 需 TensorRT 环境 + 量化校准集 |
| T28 | **SAHI 滑窗推理评估** | 低 | ✅ **已完成 (SAHI 对泛化良好模型无效, 仅恢复过拟合模型 Recall)** | SAHI (512×512, overlap=0.2) 对 10 个模型的系统评估; 对 T1/T2 过拟合模型 Recall 恢复至 91.70%, 但对 T4/T5/T7 泛化良好模型 mAP50 下降 10-21% | `python scripts/evaluate_test.py --all --sahi --slice-size 512 --overlap 0.2` |
---



## 四、实验执行流程

### 4.1 T1 — 融合架构搜索 (hidden=32, layers=3, CBAM)

**目的**: 探索增大 fusion 容量是否带来收益 (对比 Run 3 baseline: h=16,l=2,SE)

**执行步骤**:
1. 预处理缓存: `awb_dual_fusion` (AWB + Top-hat/CLAHE + Butterworth BPF/CLAHE → 3通道 [spatial, freq, orig])
2. 融合模块: `SpatialFrequencyFusion(h=32, layers=3, attn=CBAM)`, LR scale=10.0×, residual α warmup 0→0.5
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --use-fusion --fusion-hidden 32 --fusion-layers 3 --fusion-attention cbam --fusion-lr-scale 10.0 --epochs 150 --batch 16 --imgsz 640 --wandb --project ship-detection --name T1_fusion_h32_l3_cbam
   ```
4. 训练 150 epochs, 最佳在 epoch 149 (mAP50=0.9547, mAP50-95=0.6716)
5. ONNX 自动导出

**结果**: mAP50 与 Run 3 持平 (−0.2%), mAP50-95 小幅提升 (+2.6%)。融合架构扩展收益有限。

**权重**: `runs/detect/ship-detection/T1_fusion_h32_l3_cbam/weights/best.pt`

---

### 4.2 T2 — 残差强度搜索 (residual_α=0.7)

**目的**: 探索更强的融合残差连接是否提升泛化 (对比 Run 3 residual=0.5)

**执行步骤**:
1. 预处理缓存: `awb_dual_fusion`
2. 融合模块: `SpatialFrequencyFusion(h=16, layers=2, attn=SE)`, residual_target=0.7, warmup 0→0.7
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --use-fusion --fusion-hidden 16 --fusion-layers 2 --fusion-attention se --fusion-lr-scale 10.0 --fusion-residual-target 0.7 --epochs 150 --batch 16 --imgsz 640 --wandb --project ship-detection --name T2_fusion_residual07
   ```
4. 训练 150 epochs, 最佳在 epoch 142 (mAP50=0.9547, mAP50-95=0.6709)
5. ONNX 自动导出

**结果**: 与 T1 (h=32,l=3,CBAM) 完全持平 (mAP50 完全相同, mAP50-95 差 0.07%)。结论: **fusion 只要正常工作的前提下, 架构搜索与残差强度几乎不影响最终结果**。

**权重**: `runs/detect/ship-detection/T2_fusion_residual07/weights/best.pt`

---

### 4.3 T4 — 小波域杂波抑制 (DWT sub-band scaling + Fusion)

**目的**: 验证 DWT 频域预处理替代 Butterworth BPF 的效果

**执行步骤**:
1. 预处理缓存: `awb_wavelet_dual_fusion` (AWB + Top-hat/CLAHE + DWT `db4` level-1 sub-band 缩放 + 融合)
   - DWT 子带缩放: LL×0.3, LH/HL×1.5, HH×0.3
   - IDWT 重建后与 spatial 流堆叠为 3 通道 [spatial, freq, orig]
2. 融合模块: `SpatialFrequencyFusion(h=16, layers=2, attn=SE)`, residual=0.5
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_wavelet_dual_fusion --use-fusion --fusion-hidden 16 --fusion-layers 2 --fusion-attention se --fusion-lr-scale 10.0 --epochs 150 --batch 16 --imgsz 640 --wandb --project ship-detection --name T4_wavelet_dual
   ```
4. 训练 150 epochs, 最佳在 epoch 148 (mAP50=0.9549, mAP50-95=0.6537)
5. ONNX 自动导出

**结果**: mAP50 与 T2 持平, mAP50-95 低 2.6%。结论: **DWT 无参数预处理略逊于可学习 Butterworth+Fusion, 但接近性能上限**。

**权重**: `runs/detect/ship-detection/T4_wavelet_dual/weights/best.pt`

---

### 4.4 T8 — Shape-IoU 损失函数

**目的**: 验证 Shape-IoU (shape-aware 距离惩罚 + 宽高比正则) 是否优于默认 CIoU

**执行步骤**:
1. 预处理: `awb_tophat_clahe` (无 fusion, 单流预处理 — 与 Run 1 baseline 一致)
2. 损失函数替换: `--iou-loss shape-iou`, 通过 monkey-patch 替换 `v8DetectionLoss` 中的 `BboxLoss` 为 `ShapeIoUBboxLoss`
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_tophat_clahe --iou-loss shape-iou --epochs 200 --batch 16 --imgsz 640 --wandb --project ship-detection --name T8_shape_iou
   ```
4. 训练 200 epochs (patience=20, 实际 144 epochs early stop)
5. 最佳 epoch 124: mAP50=0.8843, mAP50-95=0.5772
6. ONNX 自动导出

**结果**: Shape-IoU 全面低于 CIoU (mAP50 −5.9%, Recall −9.3%)。5–30px 极小目标场景下 shape-aware 惩罚区分度有限。

**权重**: `runs/detect/ship-detection/T8_shape_iou/weights/best.pt`

---

### 4.5 Customn 数据集推理

**目的**: 验证训练好的模型在真实红外 BMP 图像上的泛化能力

**执行步骤**:
1. 选择最佳模型: T1 (best mAP50-95=0.672)
2. 推理脚本: `scripts/inference_customn_fusion.py` — 读取 Grayscale BMP → 转为 BGR 3通道 → 应用 `awb_dual_fusion` 预处理 → YOLO 预测 → 在原图上绘制检测框
3. 执行命令:
   ```
   uv run python scripts/inference_customn_fusion.py --weights "runs/detect/ship-detection/T1_fusion_h32_l3_cbam/weights/best.pt" --conf 0.25 --method awb_dual_fusion
   ```
4. 扫描 3376 张 BMP, 检出 5 张有目标 (均在 `20260523_120721/` 子目录)
5. 同时尝试 T4 (wavelet) 模型, 所有置信度阈值均无检测

**结果**: 跨域检测极具挑战 — 红外伪彩色(NSLSR-LWIR) vs 真实红外 BMP 域差异大。T1 模型仅检出 5 张, T4 模型 0 张。

**输出去向**: `runs/inference_fusion/{timestamp}/` — 仅保留有检测的图像, 附带 `detections.csv` 日志

---

### 4.6 T6 — 测试集正式评估 (T1 最佳模型)

**目的**: 获取模型在未见过的测试集上的真实泛化性能

**执行步骤**:
1. 评估脚本: `scripts/evaluate_test.py` — 使用 Ultralytics `model.val(split='test')` 直接评估
2. 评估对象: T1 最佳模型 (best mAP50-95=0.672), 使用 `awb_dual_fusion` 预处理的测试集
3. 执行命令:
   ```
   uv run python scripts/evaluate_test.py --weights "runs/detect/ship-detection/T1_fusion_h32_l3_cbam/weights/best.pt" --data "data/_preprocessed_data_awb_dual_fusion.yaml"
   ```
4. 置信度阈值: 0.001 (保留所有检测, 避免漏检)

**测试集结果 (118 张, 277 实例)**:

| 指标       | 测试集    | 验证集 (参考) |
| ---------- | --------- | ------------- |
| mAP50      | 37.67%    | 95.47%        |
| mAP50-95   | 21.23%    | 67.16%        |
| Precision  | 50.36%    | 94.60%        |
| Recall     | 39.19%    | 95.35%        |

**测试集结果 (所有实验, 118 张, 277 实例)**:

2026-05-29 使用 `scripts/evaluate_test.py --all` 执行完整测试集评估:

| # | 测试集 mAP50 | 测试集 mAP50-95 | 验证集 mAP50 | 差距 (Δ) | 说明 |
| :-: | :-: | :-: | :-: | :-: | :-- |
| Run 1 | 92.66% | 62.35% | 93.96% | −1.3% | ✅ 空域方法泛化良好 |
| Run 2 | 93.97% | 63.75% | 94.65% | −0.7% | ✅ 频域方法泛化良好 |
| Run 3 | 52.30% | 25.33% | 95.68% | **−43.4%** | ❌ Fusion 早期版本严重过拟合 |
| T8 | 86.25% | 56.71% | 88.43% | −2.2% | ✅ Shape-IoU 泛化正常 |
| T1 | 37.67% | 21.23% | 95.47% | **−57.8%** | ❌ 大融合架构严重过拟合 |
| T2 | 37.67% | 21.23% | 95.47% | **−57.8%** | ❌ 同 T1, 模型退化一致 |
| T4 | 97.45% | 66.10% | 95.49% | +2.0% | ✅ 小波+Fusion 泛化良好 |
| T5 | 97.87% | 68.02% | 97.20% | +0.7% | ✅ YOLO11l+Fusion |
| T7 | 96.63% | 65.46% | 97.23% | −0.6% | ✅ 增强抖动+Fusion |
| T10 | 93.42% | 62.01% | 94.00% | −0.6% | ✅ NWD 损失泛化正常, 未超越 CIoU |
| T11 | 96.64% | 65.72% | 95.20% | +1.4% | ✅ FECA 注意力泛化良好 |
| T12 | **97.77%** | 64.71% | 97.04% | +0.7% | ✅ C3K3 骨干替换泛化良好 |
| T14 | **97.87%** | **68.02%** | 97.00% | +0.9% | ✅ ASFF neck 泛化优秀 |
| T15 | 96.64% | 65.72% | 95.15% | +1.5% | ✅ ASFF+FECA 组合泛化正常 |
| **T16** | **98.13% 🏆** | **68.23% 🏆** | **97.85%** | **+0.3%** | ✅✨ **CRRP 增强+融合新最优!** |
| T25 | 96.47% | 68.50% | 97.11% | −0.6% | ✅ 预处理参数随机化, mAP50-95 创新高 |

**关键发现**:

1. **早期融合实验 (Run3, T1, T2) 存在严重过拟合**: 验证集 mAP50≈95% 但测试集仅 37-52%, 差距高达 43-58%。融合模块在 YOLO11m 上缺乏正则化, 退化为验证集过拟合。
2. **后验融合实验 (T4, T5, T7) 泛化优秀**: T4/小波、T5/YOLO11l、T7/增强抖动均达到 96-98% 测试 mAP50, 测试-验证差距 ≤2%。说明模型改进方向 (小波预处理、模型升级、增强抖动) 有效提升了泛化能力。
3. **Run1/Run2 是可靠的 baseline**: 无融合的预处理方法 (空域/频域) 表现出 93-94% 测试 mAP50, 验证-测试差距仅 0.7-1.3%。
4. **CRRP 数据增强突破性能天花板 (T16)**: T16 使用 2x CRRP 离线增强 + YOLO11l+Fusion 在测试集上达到 **mAP50=98.13%, mAP50-95=68.23%** 的新纪录, 验证-测试差距仅 +0.3%。CRRP 的 Copy-Rotate-Resize-Paste 策略有效增加训练样本多样性, 提升 Recall 至 95.70%。
5. **ASFF (T14) 和 T5 (CIoU) 持平**: 两者均达到 97.87%/68.02%, ASFF 在 Neck 替换方面有潜力但边际收益有限。
6. **组合实验 (T15) 未受益**: ASFF+FECA 组合与 FECA 单独使用持平 (96.64%), 说明 ASFF 和 FECA 在特征融合路径上可能存在冲突。

**最终排名 (测试集 mAP50)**:
1. **T16** — CRRP + YOLO11l + Fusion — **98.13%** 🥇🏆
2. **T5** — YOLO11l + Fusion (h=16,l=2,SE) — **97.87%** 🥈
2. **T14** — ASFF Neck — **97.87%** 🥈
4. **T12** — C3K3 骨干替换 — **97.77%** 
5. **T4** — DWT Wavelet + Fusion — **97.45%** 
6. **T25** — Preprocess jitter + YOLO11m + Fusion — **96.47%** (mAP50-95 **68.50%** 🏆 历史最高)
7. **T11** — FECA 注意力 — **96.64%**
7. **T7** — Augment jitter 0.3 + Fusion — **96.63%** 
9. **T28** — SAHI 集成 (yolo11l) — **94.63%** (SAHI无效, TTA下降)
10. Run 2 — Butterworth BPF + CLAHE — 93.97%
11. T10 — NWD 损失 — 93.42%
12. Run 1 — Top-hat + CLAHE — 92.66%

---

### 4.7 T5 — YOLO 模型升级 (YOLO11l)

**目的**: 将基线模型从 YOLO11m (20.0M 参数) 升级到 YOLO11l (25.4M 参数), 观察模型容量增加后融合模块的增益是否扩大

**基线对比**: Run 3 (YOLO11m + Fusion h=16,l=2,SE, mAP50=0.9568, mAP50-95=0.6551)

**执行步骤**:
1. 预处理缓存: `awb_dual_fusion`
2. 模型: `yolo11l.pt` (25.4M 参数, 约 YOLO11m 的 1.27×)
3. 融合模块: `SpatialFrequencyFusion(h=16, layers=2, attn=SE)` — 与 Run 3 保持一致
4. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --use-fusion --model yolo11l.pt --epochs 150 --batch 16 --imgsz 640 --wandb --project ship-detection --name T5_yolo11l_fusion
   ```
5. Batch size 验证: batch=16 正向传播 ~1.9GB activations, 训练 VRAM 约 5.5GB, 在 RTX 5080 Laptop 16GB 上安全
6. 训练 150 轮 (0.712h), 最佳 epoch 150: mAP50=**0.9720**, mAP50-95=**0.6860**
7. GPU 显存峰值: 10.2 GB / 16 GB (batch=16 安全)
8. 训练后验证结果:

| 指标       | T5 (YOLO11l+Fusion) | T1 (YOLO11m+Fusion h=32) |  差异  |
| ---------- | :-----------------: | :----------------------: | :----: |
| mAP50      |      **0.9720**     |         0.9547           | +1.7%  |
| mAP50-95   |      **0.6860**     |         0.6716           | +2.1%  |
| Precision  |       0.9576        |         0.9460           | +1.2%  |
| Recall     |       0.9392        |         0.9535           | −1.5%  |

**结果**: YOLO11l 容量升级显著持续提升融合效果, 创下 mAP50=0.972 的新纪录。验证了 (a) YOLO 容量的增大与融合模块协同; (b) 融合固定参数 h=16,l=2,SE,residual=0.5 对 YOLO11l 同样适用。

**权重**: `runs/detect/ship-detection/T5_yolo11l_fusion/weights/best.pt`

---

### 4.8 T7 — 数据增强消融 (随机增强参数)

**目的**: 对增强参数施加回合级随机抖动, 增加训练多样性, 缓解小数据集过拟合

**原理**: 标准 YOLO 训练中, 增强参数在全部 epochs 中保持不变。T7 在每个 epoch 开始时, 对 `scale`, `hsv_h/s/v`, `translate`, `degrees`, `shear` 等参数施加 ±30% 的随机抖动, 使模型在不同增强强度下学习, 提升泛化鲁棒性。

**基线对比**: Run 3 (awb_dual_fusion + Fusion h=16,l=2,SE, 无增强抖动, mAP50=0.9568, mAP50-95=0.6551)

**执行步骤**:
1. 预处理缓存: `awb_dual_fusion`
2. 融合模块: `SpatialFrequencyFusion(h=16, layers=2, attn=SE)` — 与 Run 3 保持一致
3. 增强抖动: `--augment-jitter 0.3` (所有增强参数随机 ±30%)
4. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --use-fusion --augment-jitter 0.3 --epochs 150 --batch 16 --imgsz 640 --wandb --project ship-detection --name T7_augment_jitter
   ```
5. 训练 150 epochs, 记录最佳指标

**实现细节**:
- 回调函数 `_make_augment_jitter_callback(magnitude)` 注册到 `on_epoch_start`
- 每个 epoch 开始时, 对 `cfg.scale`, `cfg.hsv_h/s/v`, `cfg.translate`, `cfg.degrees`, `cfg.shear` 应用随机乘法抖动: `param *= 1.0 + uniform(-m, m) * m`
- 抖动后确保增强参数不小于 0

**训练结果**: Early stopped 138 epochs (0.511h), 最佳 epoch 118:

| 指标       | T7 (增强抖动) | Run 3 (无抖动) |  差异  |
| ---------- | :----------: | :-----------: | :----: |
| mAP50      |   **0.9723** |    0.9568     | +1.6%  |
| mAP50-95   |    0.6724    |    0.6551     | +2.6%  |
| Precision  |    0.9556    |    0.9569     | −0.1%  |
| Recall     |   **0.9606** |    0.9532     | +0.8%  |

**分析**: 增强参数抖动在 YOLO11m+Fusion 基础上进一步提升了 Recall (+0.8%) 和 mAP50 (+1.6%)。值得注意的是 T7 使用 YOLO11m 即达到了与 T5 (YOLO11l) 几乎相同的 mAP50 (0.972 vs 0.972), 说明增强抖动对小模型的正则化效果非常显著。然而 mAP50-95 增益 (2.6%) 并未达到 T5 的 4.7%, 表明模型升级 + 融合仍是提升定位精度的首选。

**权重**: `runs/detect/ship-detection/T7_augment_jitter/weights/best.pt`

### 4.9 T10 — NWD 损失函数 (Wasserstein Distance)

**目的**: 验证 Normalized Wasserstein Distance (NWD) 替代 CIoU 对 5-30px 极小目标的提升效果

**改进算法**: 传统 IoU 损失对像素级重叠敏感, 当目标仅 5-30px 时, 预测框和真实框的微小偏移会导致 IoU 剧烈变化。NWD 将边界框建模为 2D 高斯分布 (中心=均值, 宽高=方差), 用 Wasserstein 距离度量两个分布之间的差异。具体地, 对预测框 A=(cx_a,cy_a,w_a,h_a) 和真实框 B=(cx_b,cy_b,w_b,h_b), 归一化 Wasserstein 距离定义为:
```
NWD(A,B) = exp(-√(W₂²(A,B)) / C)
其中 W₂² = ||center_a - center_b||² + ||(w_a/2, h_a/2) - (w_b/2, h_b/2)||²
C = 12.8 为归一化常数 (与数据集目标尺度相关)
```
NWD 对亚像素级偏移不敏感, 理论上更适合极小目标检测。NWD Loss = 1 − NWD(A,B)。本实验使用 NWD 完全替代 CIoU, 保持分类和 DFL 损失不变。

**执行步骤**:
1. 预处理: `awb_dual_fusion`, 融合模块 `SpatialFrequencyFusion(h=16,l=2,SE)`
2. 模型: YOLO11l, 损失函数: NWD (const=12.8)
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --model yolo11l.pt --iou-loss nwd --epochs 150 --batch 16 --imgsz 640 --wandb --name T10_nwd
   ```
4. 训练 150 epochs, 最佳 epoch ~96: val mAP50=0.940, mAP50-95=0.636

**测试集结果**:
| 指标 | T10 (NWD) | T5 (CIoU) | 差异 |
| --- | :---: | :---: | :---: |
| mAP50 | 93.42% | 97.87% | −4.45% |
| mAP50-95 | 62.01% | 68.02% | −6.01% |

**结论**: NWD 在 NSLSR-LWIR 数据集上未超越 CIoU, 可能因为伪标签精度有限或 NWD 适合更极端的小目标场景 (<5px)。

**权重**: `runs/detect/ship-detection/T10_nwd/weights/best.pt`

---

### 4.10 T11 — C2f-FECA 注意力插入骨干

**目的**: 验证 Frequency Enhanced Channel Attention (FECA) 在 YOLO11l 骨干每层后插入的效果

**改进算法**: FECA (Frequency Enhanced Channel Attention) 在传统通道注意力的基础上引入频域信息。传统通道注意力 (如 SE) 只使用全局平均池化得到的标量描述每个通道, 丢失了频率分布信息。FECA 的改进在于:
1. 对每个通道的特征图做 FFT, 将空间域特征变换到频域
2. 使用可学习的频率权重向量对频域分量加权 (增强目标相关的频率, 抑制杂波频率)
3. iFFT 将处理后的频域特征变换回空间域
4. 再接标准 SE 式通道注意力 (GAP → FC → ReLU → FC → Sigmoid → 通道加权)

FECA 模块极轻量, 每模块仅 5 个可学习参数 (频率加权向量), 插入在骨干每个 C3k2 模块之后 (共 4 处), 几乎不增加推理开销。

**执行步骤**:
1. 预处理: `awb_dual_fusion`
2. 模型: YOLO11l + 4× FECA (骨干每层后)
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --model yolo11l.pt --attention-type feca --epochs 150 --batch 16 --imgsz 640 --wandb --name T11_feca
   ```

**测试集结果**:
| 指标 | T11 (FECA) | T5 (no attn) | 差异 |
| --- | :---: | :---: | :---: |
| mAP50 | 96.64% | 97.87% | −1.23% |
| mAP50-95 | 65.72% | 68.02% | −2.30% |

**结论**: FECA 注意力提供了一定的特征增强但未超越基线。可能原因: (a) 5 参数 FECA 容量不足以影响 YOLO11l 大模型; (b) 频域通道注意力对红外舰船检测的收益有限。

**权重**: `runs/detect/ship-detection/T11_feca/weights/best.pt`

---

### 4.11 T12 — C3K3 骨干替换 (C3k2 → C3k, k=3)

**目的**: 验证 C3-style 拓扑 (CSP bottleneck with 3 convolutions) 替换 C2f-style C3k2 的效果

**改进算法**: YOLO 系列中 Neck 的特征提取块有两种主流拓扑:
- **C2f (YOLOv8/v11)**: `cv1(1×1→2c)` → 分两路 → 一路直接到 Concat, 另一路过 n 个 Bottleneck → Concat → `cv2(1×1)`
- **C3 (YOLOv5)**: `cv1(1×1→c)` → n 个 Bottleneck → Concat + `cv2(1×1)` 残差跳跃 → `cv3(1×1)`

C3 的关键差异是有一个额外的 1×1 卷积 (cv2) 作为"跳跃路径" (skip path), 将输入特征直接映射到输出通道并与 Bottleneck 输出拼接, 形成类似残差连接的结构。这种结构在理论上可以更好地保留原始特征信息。

本实验将 YOLO11l 骨干+Neck 中所有 8 个 C3k2 模块替换为 C3k 模块, 且 Bottleneck 使用 3×3 卷积 (k=3)。替换后参数量从 25.4M 增至 28.9M (+13.8%)。

**执行步骤**:
1. 预处理: `awb_dual_fusion`
2. 模型: YOLO11l + C3K3
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --model yolo11l.pt --neck-type c3k3 --epochs 150 --batch 16 --imgsz 640 --wandb --name T12_c3k3
   ```

**测试集结果**:
| 指标 | T12 (C3K3) | T5 (default) | 差异 |
| --- | :---: | :---: | :---: |
| mAP50 | 97.77% | 97.87% | −0.10% |
| mAP50-95 | 64.71% | 68.02% | −3.31% |

**结论**: C3K3 提升了 mAP50 但 mAP50-95 下降, C3-style 拓扑增加参数量 (28.9M vs 25.4M) 但未带来定位精度提升。

**权重**: `runs/detect/ship-detection/T12_c3k3/weights/best.pt`

---

### 4.12 T14 — ASFF 自适应空间特征融合 (Neck)

**目的**: 验证 ASFF (Adaptively Spatial Feature Fusion) 替换 YOLO Neck 中 Concat 操作的效果

**改进算法**: YOLO Neck 的 FPN 结构通过 Concat 操作融合不同尺度的特征图 (如 P3/P4/P5 → P4 检测头)。传统 Concat 在通道维度简单拼接, 等权处理所有空间位置。ASFF 的改进在于可学习的**逐像素加权融合**:
1. 对每个输入特征图 (来自不同尺度), 通过 1×1 卷积调整到相同通道数
2. 对每个尺度分支学习一个空间注意力权重图 (尺寸与目标特征图相同)
3. 三个权重图经过 Softmax 归一化 (确保同一位置权重和为 1)
4. 加权求和得到融合特征图

即对于位置 (i,j), 融合特征 = α₁·F₁ + α₂·F₂ + α₃·F₃, 其中 α₁,α₂,α₃ = Softmax(网络学习)。这使得网络可以**自适应地**决定每个空间位置应该更关注哪个尺度的特征 — 小目标位置更关注高分辨率特征, 大目标位置更关注语义特征。

本实验将 YOLO11l Neck 中的 4 个 Concat 替换为 ASFF 模块, 使用 lazy init 在第一次前向时根据实际张量形状初始化参数, 避免通道维度硬编码。

**执行步骤**:
1. 预处理: `awb_dual_fusion`
2. 模型: YOLO11l + ASFF
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --model yolo11l.pt --neck-type asff --epochs 150 --batch 16 --imgsz 640 --wandb --name T14_asff
   ```

**测试集结果**:
| 指标 | T14 (ASFF) | T5 (default neck) | 差异 |
| --- | :---: | :---: | :---: |
| mAP50 | 97.87% | 97.87% | 0% |
| mAP50-95 | 68.02% | 68.02% | 0% |

**结论**: ASFF 结果为与 T5 完全持平。虽然 params/GFLOPs 相同, ASFF 的加权融合策略在标准数据集上与 Concat 无显著差异。可能 ASFF 在复杂背景场景下更有用。

**权重**: `runs/detect/ship-detection/T14_asff/weights/best.pt`

---

### 4.13 T15 — ASFF + FECA 组合实验

**目的**: 验证 ASFF (Neck) + FECA (骨干注意力) 联合使用的叠加收益

**改进算法**: 本实验尝试将两种不同粒度的特征优化方法组合:
- **ASFF (Neck 层面)**: 在特征金字塔融合阶段, 用逐像素加权融合替代 Concat, 优化**跨尺度特征融合策略**
- **FECA (骨干层面)**: 在骨干网络每层输出后, 用频域通道注意力重新校准特征图, 优化**单尺度特征表达**

两者作用于网络的不同阶段 (骨干 vs Neck), 理论上应具有互补性。本实验同时应用 `--neck-type asff` 和 `--attention-type feca`, 期望 1+1>2。

**执行步骤**:
1. 预处理: `awb_dual_fusion`
2. 模型: YOLO11l + ASFF + FECA
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --model yolo11l.pt --neck-type asff --attention-type feca --epochs 150 --batch 16 --imgsz 640 --wandb --name T15_asff_feca
   ```

**测试集结果**:
| 指标 | T15 (ASFF+FECA) | T14 (ASFF alone) | T11 (FECA alone) |
| --- | :---: | :---: | :---: |
| mAP50 | 96.64% | 97.87% | 96.64% |
| mAP50-95 | 65.72% | 68.02% | 65.72% |

**结论**: ASFF+FECA 完全与 FECA 单独使用持平, 且低于 ASFF 单独使用。说明 ASFF 和 FECA 在特征优化路径上存在冲突, 两者叠加无正向收益。

**权重**: `runs/detect/ship-detection/T15_asff_feca/weights/best.pt`

---

### 4.14 T16 — CRRP 数据增强 (Copy-Rotate-Resize-Paste)

**目的**: 验证离线数据增强 CRRP (从训练集中提取舰船实例, 旋转+缩放+粘贴到背景) 的效果

**改进算法**: CRRP (Copy-Rotate-Resize-Paste) 是一种针对小目标检测的离线数据增强策略, 核心思想是从训练图像中**复制**小目标实例, 经过**旋转**和**缩放**后,**粘贴**到其他图像上, 同时自动更新标注。具体流程:
1. **构建舰船库**: 遍历 803 张训练图像, 提取所有舰船实例的像素块 (共 973 个), 宽高过滤保留 8-60px 范围
2. **每张图像生成 2 个增强版本**: 
   - 复制原图作为背景
   - 从舰船库随机选择 1-2 个实例
   - 对每个实例: 随机缩放 (10-50px 宽 × 5-20px 高), 随机旋转 (-30°~+30°), 随机粘贴位置
   - 以 85%:15% 的透明度与原图融合 (避免生硬边缘)
   - 更新标签文件, 同时保留原图像中的舰船标注
3. **输出**: 1606 张增强训练图, 验证集和测试集保持不变

CRRP 与 YOLO 内置的 Mosaic/MixUp 在线增强正交互补 — 在线增强在批次层面组合已有图像, CRRP 在源数据层面创造新的训练样本。

**执行步骤**:
1. 生成增强数据:
   ```
   uv run python scripts/crrp_augment.py --input-dir data/processed_nslsr/lwir --output-dir data/crrp_augmented --copies 2 --max-instances 2 --control-ratio 0.0
   ```
2. 训练命令:
   ```
   uv run python train.py --data configs/data_crrp_augmented.yaml --model yolo11l.pt --use-fusion --preprocess-method awb_dual_fusion --epochs 150 --batch 16 --imgsz 640 --wandb --name T16_crrp
   ```

**测试集结果**:
| 指标 | T16 (CRRP) | T5 (no CRRP) | 提升 |
| --- | :---: | :---: | :---: |
| mAP50 | **98.13% 🏆** | 97.87% | **+0.26%** |
| mAP50-95 | **68.23% 🏆** | 68.02% | **+0.21%** |
| Recall | **95.70%** | 93.92% | **+1.78%** |

**结论**: CRRP 数据增强效果显著 — Recall 提升 1.78%, mAP50 和 mAP50-95 均创新高。说明离线 Copy-Rotate-Resize-Paste 对缓解红外舰船小样本过拟合非常有效。**当前最佳模型** 🏆

**权重**: `runs/detect/ship-detection/T16_crrp/weights/best.pt`

---

### 4.15 T19 — EMA/CBAM 注意力插入 Neck (C2EMA)

**目的**: 验证 C2EMA (Efficient Multi-Scale Attention) 替换 YOLO Neck FPN 中 C2f 模块的效果

**改进算法**: C2EMA 是一种将 EMA (Efficient Multi-Scale Attention) 集成到 C2f 结构中的轻量 Neck 改进方案。核心思想是用 EMA 注意力替换 C2f 中的 Bottleneck, 使 Neck 在融合多尺度特征时具备跨空间维度的注意力感知能力:
1. 对 Neck 中每个 C2f 模块, 将其 Bottleneck 部分替换为 EMA 注意力单元
2. EMA 将特征图沿通道分组, 在每组内分别计算 1×1 分支和 3×3 分支的跨空间注意力
3. 1×1 分支保留全局上下文, 3×3 分支捕获局部空间细节, 二者通过 Sigmoid 门控融合
4. 保留 C2f 的残差连接和 Concat 融合结构, 仅替换特征提取子模块

C2EMA 对标 T14 (ASFF) 的技术路线 — 都是改进 Neck 的特征融合能力, 但 ASFF 改进 Concat (跨尺度融合), C2EMA 改进 C2f (内部特征提取)。本实验将 Neck 中所有 4 个 C2f-K 模块替换为 C2EMA 模块。

**执行步骤**:
1. 预处理: `awb_dual_fusion`
2. 模型: YOLO11l + C2EMA (Neck 4 个 C2f 替换)
3. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --model yolo11l.pt --neck-type c2ema --epochs 150 --batch 16 --imgsz 640 --wandb --project ship-detection --name T19_c2ema
   ```

**测试集结果**:
| 指标 | T19 (C2EMA) | T14 (ASFF) | T5 (baseline) | 差异 |
| --- | :---: | :---: | :---: | :---: |
| mAP50 | 96.27% | 97.87% | 97.87% | −1.60% |
| mAP50-95 | 67.92% | 68.02% | 68.02% | −0.10% |
| Precision | 94.71% | — | — | — |
| Recall | 93.92% | — | — | — |

**结论**: C2EMA 在 mAP50 上比 ASFF 低 1.60%, 在 mAP50-95 上基本持平 (−0.10%)。C2EMA 替换 Neck 中 C2f 模块的 EMA 注意力方案未超越 ASFF 的 Concat 替换方案。可能原因: (a) EMA 注意力在特征提取阶段的增幅不足以弥补替换 Bottleneck 带来的容量损失 (C2EMA 参数量 25.3M vs 默认 25.3M, 但 EMA 注意力参数效率低于 Bottleneck); (b) Neck 的 C2f 内部特征提取改进不如跨尺度融合 (ASFF) 有效。

**权重**: `runs/detect/ship-detection/T19_c2ema/weights/best.pt`

---

### 4.16 T23 — TTA 测试增强

**目的**: 验证 YOLO 内置测试时增强 (Test-Time Augmentation) 在红外舰船检测任务上的有效性

**改进算法**: YOLO 内置 TTA 在推理阶段对每张测试图像应用多尺度变换和水平翻转, 通过 NMS 融合多组预测结果。具体流程:
1. **多尺度推理**: 以 {0.5, 0.75, 1.0, 1.25, 1.5} × imgsz=640 共 5 个尺度分别推理
2. **水平翻转**: 每个尺度同时推理原始图像和水平翻转图像
3. **预测融合**: 所有 10 组预测通过 NMS (IoU=0.65) 合并为最终输出

TTA 无需重新训练, 直接在已有模型上通过 `model.val(augment=True)` 启用。

**执行步骤**:
1. 使用 `scripts/evaluate_tta.py` 对 T16 (CRRP, 当前最优) 和 T5 (基准) 分别评估标准/TTA
2. 对比标准推理与 TTA 推理的测试集指标

**测试集结果**:

| 指标 | T16 标准 | T16 TTA | Δ | T5 标准 | T5 TTA | Δ |
| --- | :---: | :---: | :---: | :---: | :---: | :---: |
| mAP50 | **98.13%** | 97.82% | **−0.31%** | **97.87%** | 97.07% | **−0.80%** |
| mAP50-95 | **68.23%** | 68.01% | **−0.22%** | **68.02%** | 66.61% | **−1.41%** |
| Recall | **95.70%** | 95.67% | −0.03% | **94.58%** | 94.22% | −0.36% |
| Precision | 94.98% | 94.79% | −0.19% | **97.07%** | 93.20% | −3.87% |

**结论**: TTA 在两个模型上均造成性能下降, 对红外舰船检测无效。原因分析:
1. **预处理归一化**: awb_dual_fusion 预处理已大幅标准化图像统计量, TTA 的多尺度/翻转引入的额外方差大于信息增益
2. **小目标特性**: 5-30px 目标经过多尺度缩放到 0.5× 时会进一步缩小至不可识别, 引入噪声预测
3. **CRRP 增强替代**: T16 的离线 Copy-Rotate-Resize-Paste 增强已提供充分的尺度多样性, 与 TTA 的功能重叠

**结论: TTA 在红外舰船检测任务中不适用, 不建议作为后续实验的标准组件。**

**权重**: `-`

---

### 4.15 SAHI 滑窗推理评估 (T20)

**目的**: 验证 SAHI (Slicing Aided Hyper Inference) 滑窗推理是否能通过将大图切分为子图来提升极小目标 (5-30px) 的检测 Recall, 特别是对过拟合模型是否有恢复效果。

**实验设置**:
- 切片大小: 512×512 (略小于原始 640×480 图像尺寸, 每个图像约 2 个切片, 20% 重叠)
- 推理置信度: 0.001 (与标准评估一致, 避免漏检)
- SAHI 后处理: NMS (IoU=0.45) 合并重叠切片的重复检测
- 评估脚本: `scripts/evaluate_test.py --all --sahi --slice-size 512 --overlap 0.2`
- 测试集: NSLSR-LWIR (118 张, 277 个目标实例)
- 对照: 标准推理 (无 SAHI)

**SAHI 评估结果 (slice=512, overlap=0.2)**:

| # | 标准 mAP50 | 标准 Recall | SAHI mAP50 | SAHI mAP50-95 | SAHI Precision | SAHI Recall | Δ mAP50 | 说明 |
| :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-- |
| Run 1 | 92.66% | 90.52% | 30.84% | 16.59% | 57.41% | 33.57% | **−61.82%** | ❌ SAHI 严重降低空域方法性能 |
| Run 2 | 93.97% | 93.02% | 61.34% | 32.79% | 18.59% | 71.48% | **−32.63%** | ❌ SAHI 降低频域方法 Recall 损失较小 |
| Run 3 | 52.30% | 95.32% | 14.92% | 6.49% | 7.70% | 22.02% | **−37.38%** | ❌ 过拟合 Fusion 模型 SAHI 无法恢复 |
| T1 | 37.67% | 39.19% | **89.75%** | **59.78%** | 51.63% | **91.70%** | **+52.08%** | ✅ SAHI 大幅恢复过拟合 T1 Recall! |
| T2 | 37.67% | 39.19% | **89.75%** | **59.78%** | 51.63% | **91.70%** | **+52.08%** | ✅ SAHI 大幅恢复过拟合 T2 Recall! |
| T4 | 97.45% | 93.87% | 87.49% | 56.56% | 34.69% | 89.17% | −9.96% | ⚠️ SAHI 略微降低泛化良好的 T4 |
| T5 | 97.87% | 94.58% | 77.24% | 46.95% | 46.49% | 81.23% | −20.63% | ❌ SAHI 降低 YOLO11l+Fusion 性能 |
| T7 | 96.63% | 96.06% | 79.45% | 50.56% | 56.05% | 81.95% | −17.18% | ❌ SAHI 降低增强抖动模型性能 |
| T8 | 86.25% | 82.11% | 8.97% | 4.31% | 5.01% | 14.80% | **−77.28%** | ❌ SAHI 完全破坏 Shape-IoU 模型 |
| T9 | 93.18% | 86.90% | 59.81% | 35.71% | 15.09% | 71.48% | −33.37% | ❌ SAHI 降低 WIoU 模型性能 |

**关键发现**:

1. **SAHI 对过拟合模型有显著的 Recall 恢复效果**: T1 和 T2 (测试集 mAP50 仅 37.67%) 在 SAHI 推理下 mAP50 飙升至 **89.75%**, Recall 从 39.19% 提升至 **91.70%**。SAHI 的滑窗机制使得模型在每个子图上面对更少的杂波和更突出的目标, 有效缓解了 fusion 模块对全图统计特征的过拟合依赖。
2. **对泛化良好的模型, SAHI 反而降低性能**: T4/T5/T7 (标准 mAP50 96-98%) 的 SAHI mAP50 均下降 10-21%。原因: (a) 切片边界切割目标导致碎片化检测; (b) 重叠区域产生大量重复预测, NMS 合并后可能漏掉低置信度的真实目标; (c) 原始 640×480 图像中目标已足够清晰, 滑窗引入的额外分辨率增益不抵信息损失。
3. **SAHI 降低 Precision**: 所有模型的 SAHI Precision 均显著低于标准推理。T8 Precision 跌至 5.01%, Run 2 Precision 从 97.51% 降至 18.59%。滑窗推理产生大量冗余检测框, NMS 无法完全消除。
4. **SAHI 对训练不充分的模型 (Run3, T8) 无恢复效果**: 即使 SAHI 提供了更多检测机会, 模型本身的检测能力仍然不足。
5. **单切片数量不足**: 640×480 图像使用 512×512 滑动切片仅产生约 2 个切片, SAHI 的增益空间有限。如果使用更小的切片 (如 320×320), SAHI 可能更有帮助, 但推理开销将成倍增加。

**总体结论**: SAHI 滑窗推理在 NSLSR-LWIR 测试集上对**泛化良好的模型不适用** (mAP50 下降 10-62%)。它仅对严重过拟合的融合模型 (T1/T2) 有显著的 Recall 恢复效果, 但这只是治标不治本 — 根本方案是提升模型本身的泛化能力。**不建议将 SAHI 作为标准推理组件**, 但对过拟合模型的分析诊断有帮助。

**输出**: `yolo-training/results/sahi_eval.json`

---

### 4.16 P2 小目标检测头冒烟测试

**目的**: 验证在 YOLO11m 上添加 P2 小目标检测头 (160×160 特征图) 是否能提升 5-30px 极小目标的检测性能。P2 头在标准 YOLO11 (P3/P4/P5) 基础上增加一个 4×下采样的检测层, 理论上对极小目标的定位更精确。

**实验设置**:
- 模型: `yolo11m_p2.yaml` (Ultralytics 官方 P2 配置, 在 P3/P4/P5 基础上增加 P2 160×160 检测头)
- 融合模块: `SpatialFrequencyFusion(h=16, layers=2, attn=SE)` 附加到骨干
- 输入尺寸: 640×640 (与所有实验保持一致)
- Batch size 探索: 16 → 8 → 4 → 2
- 训练脚本: `scripts/train_p2.ps1`

**执行过程**:

1. **batch=16 尝试**: OOM — P2 头增加了一个 160×160 检测头, 特征图面积是 P5 (20×20) 的 64 倍, 激活内存占用激增。
2. **batch=8 尝试**: OOM — 显存峰值在正向传播阶段即超出 16GB, 无法分配到足够内存。
3. **batch=4 尝试**: OOM — 即使 batch=4, P2+P3+P4+P5 四个检测头的特征图总激活量仍超出 16GB VRAM。
4. **batch=2 尝试**: 训练启动但极其缓慢 — 每个 step 超过 8 秒, 预估 200 epochs 需约 111+ 小时。**无法在合理时间内完成训练**。

**原因分析**:
- P2 (160×160) 特征图 = 25,600 个网格单元, 是 P5 (20×20=400) 的 64 倍
- 每个网格单元需要存储分类损失、回归损失和 DFL 损失的中间激活
- P2 头引入的额外显存开销约 6-8GB, 与 base YOLO11m (~5.5GB) 叠加后远超 16GB
- Fusion 模块额外增加 ~0.3GB, 进一步加剧显存压力
- 即使采用 gradient checkpointing (梯度检查点), P2 的 forward 激活无法被压缩到 16GB 以内

**结论**: **P2 小目标检测头在当前 16GB VRAM 限制下不可行。** 640×640 输入 + YOLO11m + P2 四个检测头 + Fusion 的总显存需求约 18-22GB, 远超 RTX 5080 Laptop 16GB 的容量。该方向**已取消**, 不纳入后续实验规划。

**替代方案**:
1. **缩小输入尺寸**: 将 imgsz 从 640 降至 480 可减少 P2 特征图面积约 44%, 但会降低小目标的像素分辨率, 得不偿失。
2. **使用更小的模型**: YOLO11n/s 可降低基础显存占用, 但模型容量不足以从 P2 头获益。
3. **升级硬件**: 24GB+ VRAM (RTX 4090/5090) 可支持 P2 头训练。
4. **不使用 P2 头的等效方案**: CRRP 数据增强 (T16) 已通过离线 Copy-Rotate-Resize-Paste 提供了小目标多样性, 达到 **98.13% mAP50** 的最优水平, 远优于 P2 头可能带来的边际收益。

---

### 4.17 T25 — 预处理参数随机化 (preprocess-jitter)

**目的**: 验证对预处理阶段的参数 (CLAHE clip_limit/grid_size, top-hat kernel_size, Butterworth cutoff 等) 施加 epoch 级随机化是否能进一步提升模型泛化能力。

**改进算法**: 与 T7 (增强参数随机化) 补充 — T7 对 YOLO 在线增强参数 (scale/hsv/translate 等) 施加 ±30% 抖动, 而 T25 对**离线预处理**阶段的参数施加随机化:
1. **CLAHE**: `clip_limit` 在 [1.5, 3.0] 均匀采样, `grid_size` 在 [6, 10] 均匀采样
2. **Top-hat**: `kernel_size` 在 [15, 45] 均匀采样, `light_bg` True/False 随机选择
3. **Butterworth BPF**: `cutoff` 在 [15, 45] 均匀采样, `order` 在 [2, 4] 均匀采样

每个 epoch 独立采样一组参数, 重新生成预处理后的图像批次, 使模型在每个 epoch 面对略有不同的预处理统计量。

**基线对比**: Run 3 / T7 (YOLO11m + Fusion h=16,l=2,SE, awb_dual_fusion 预处理)

**执行步骤**:
1. 预处理方法: `awb_dual_fusion` + `--preprocess-jitter` (自适应参数随机化)
2. 融合模块: `SpatialFrequencyFusion(h=16, layers=2, attn=SE)` — 与 Run 3/T7 保持一致
3. 模型: YOLO11m
4. 训练命令:
   ```
   uv run python train.py --dataset nslsr-lwir --preprocess --preprocess-method awb_dual_fusion --use-fusion --preprocess-jitter --epochs 150 --batch 16 --imgsz 640 --wandb --project ship-detection --name T25_preprocess_jitter
   ```

**训练结果**: Early stopped at epoch 143 (0h), 最佳 epoch 123:

| 指标 | T25 (preprocess-jitter) | T7 (augment-jitter) | Run 3 (无抖动) |
| --- | :---: | :---: | :---: |
| mAP50 | 0.9711 | **0.9723** | 0.9568 |
| mAP50-95 | **0.6854** | 0.6724 | 0.6551 |
| Precision | 0.9499 | **0.9556** | 0.9569 |
| Recall | 0.9491 | **0.9606** | 0.9532 |

**验证集分析**: T25 的 mAP50 (0.9711) 与 T7 (0.9723) 几乎持平, 但 mAP50-95 (0.6854) 超越了 T7 (0.6724) 和 T5 (0.6860)。说明预处理参数随机化对**定位精度**的提升优于在线增强参数随机化。但 Recall (0.9491) 低于 T7 (0.9606), 说明在线增强参数随机化对**召回率**提升更强。

**测试集结果**:

| 指标 | T25 (preprocess-jitter) | T7 (augment-jitter) | T5 (YOLO11l baseline) |
| --- | :---: | :---: | :---: |
| mAP50 | 96.47% | 96.63% | **97.87%** |
| mAP50-95 | **68.50% 🏆** | 65.46% | 68.02% |
| Precision | **97.00%** | — | 97.07% |
| Recall | 93.25% | **96.06%** | 94.58% |

**关键发现**:
1. **mAP50-95 创历史新高 (68.50%)** 🏆 — T25 用 YOLO11m 即达到比 T16 (68.23%, YOLO11l+CRRP) 更高的 mAP50-95, 验证了预处理参数随机化对定位精度的独特贡献
2. **Recall 较低**: 93.25% 显著低于 T7 (96.06%), 预处理随机化在 Recall 上不如在线增强参数随机化
3. **与 T5 对比**: YOLO11m+preprocess-jitter (96.47%) < YOLO11l (97.87%), 模型容量差距无法通过预处理随机化弥补
4. **与 T7 对比**: preprocess-jitter vs augment-jitter 两种随机化策略不完全互补 — preprocess-jitter 偏向提升定位 (mAP50-95 更高), augment-jitter 偏向提升召回 (Recall 更高)

**结论**: 预处理参数随机化是一种有效的正则化手段, mAP50-95 创历史新高 (68.50%)。但 Recall 下降明显, 且无法完全弥补模型容量差距。**建议后续实验中作为标准预处理选项保留, 优先与 T16 CRRP 结合使用。**

**权重**: `runs/detect/ship-detection/T25_preprocess_jitter/weights/best.pt`

---

## 五、关键结论

1. **频域 Precision 最优****: Butterworth BPF 有效抑制周期性海杂波, Precision=0.9751 (三 Run 最高)
2. **融合 Recall 提升最大**: Fusion 相比空域 Recall +4.8%, 相比频域 +2.3%
3. **融合综合最优**: mAP50 和 mAP50-95 均为最高, 无明显 trade-off
4. **融合开销极小**: 仅 3,361 参数 (0.017% 总量), 推理耗时 3.1ms
5. **空域方法 baseline**: Simple top-hat 已能获得 0.9396 mAP50, 说明形态学预处理对红外舰船检测非常有效
6. **Shape-IoU 未超越 CIoU**: T8 实验表明 Shape-IoU 在 NSLSR-LWIR 数据集上各指标全面低于默认 CIoU (mAP50−5.9%, Recall−9.3%)。可能原因：(a) 当前目标尺度非常小(5–30px), shape-aware 距离惩罚对高度子像素化的目标区分度有限；(b) CIoU 的 center-to-center 距离项对小目标定位已足够有效
7. **融合架构升级增益有限**: T1 (hidden=32, layers=3, CBAM) 相比 Run 3 (hidden=16, layers=2, SE) 在 mAP50 上持平 (−0.2%), mAP50-95 小幅提升 (+2.6%)。增大 fusion 容量并未带来显著收益, 原始小型 fusion 已充分捕获空频互补信息
8. **残差强度增到 0.7 后结果与 T1 完全持平**: T2 (residual_α=0.7, h=16, l=2, SE, 3.4K 参数) vs T1 (h=32, l=3, CBAM, 8.6K 参数) 在 mAP50 完全相同, mAP50-95 仅差 0.07%。结论: **fusion 架构搜索与残差强度搜索对最终结果几乎无影响** — 只要 fusion 存在且正常工作, 性能即接近上限; 后续可固定 h=16, l=2, SE, residual=0.5
9. **小波域杂波抑制 (DWT) 结果与 Butterworth+Fusion 基本持平**: T4 使用 `db4` 1 级 DWT 子带系数缩放 (LL*0.3, LH/HL*1.5, HH*0.3) 替代 Butterworth BPF, 与 h=16,l=2,SE 融合配合, 获得 mAP50=0.955, mAP50-95=0.654。与 T2 (Butterworth+Fusion) 相比 mAP50 持平, mAP50-95 低 2.6%。结论: **DWT 子带缩放作为一种无参数频域预处理, 性能略逊于可学习的 Butterworth+Fusion 双流架构, 但接近性能上界 — 频域预处理方法之间的差异远小于 "有无频域处理" 的差异**
10. **YOLO11l 容量升级持续提升融合效果 (T5)**: T5 (YOLO11l + Fusion h=16,l=2,SE) 创下 mAP50=**0.9720**, mAP50-95=**0.6860** 的新纪录。相比 T1 (YOLO11m + Fusion h=32), mAP50 +1.7%, mAP50-95 +2.1%。验证了 (a) 骨干容量增大与融合模块协同增益, (b) 融合固定参数 (h=16,l=2,SE,residual=0.5) 对 YOLO11l 同样适用。**模型升级是当前最有效的单一改进手段**。
11. **增强参数抖动显著提升 Recall 和 mAP50 (T7)**: T7 (augment-jitter=0.3 + Fusion + YOLO11m) 以 YOLO11m 即达到 mAP50=**0.9723** (与 T5 YOLO11l 持平), Recall 提升至 **0.9606** (所有实验最高)。说明增强参数随机化对小模型的正则化效果非常显著, 但 mAP50-95 增益 (2.6% vs Run 3) 低于 T5 的 4.7%, **模型升级 + 融合仍是提升定位精度的首选方案**。
12. **测试集泛化能力是筛选实验的关键标准**: 完整测试集评估揭示了早期融合实验 (Run3, T1, T2) 存在 43-58% 的严重过拟合; 而 T4/T5/T14/T16 在测试集上达到 97-98% mAP50, 验证-测试差距 ≤2%。**最终排名**: T16 (98.13%) > T5/T14 (97.87%) > T12 (97.77%) > T4 (97.45%) > T11/T15 (96.64%)。测试集结果与验证集排名完全不同 — 验证集上 Run3/T1/T2 均显示 95%+, 但实际泛化极差。**结论: 必须用测试集验证才能得出可靠结论。**
13. **CRRP 离线数据增强是当前最有效的单一改进手段 (T16)**: T16 (CRRP + YOLO11l+Fusion) 达到 **mAP50=98.13%, mAP50-95=68.23%**, 均创历史新高。2x 离线增强 + YOLO 在线增强的正交组合有效缓解了小样本过拟合, Recall 提升至 95.70%。
14. **预处理参数随机化创 mAP50-95 历史新高 (T25)**: T25 (preprocess-jitter + YOLO11m+Fusion) 在测试集上达到 **mAP50-95=68.50%** 🏆, 超越 T16 (68.23%) 和 T5 (68.02%), 是所有实验中最高。但 Recall (93.25%) 显著低于 T7 (96.06%), 预处理随机化偏向提升定位精度而非召回率。**两种随机化策略互补**: preprocess-jitter 提升 mAP50-95, augment-jitter 提升 Recall, 建议在后续实验中同时启用。
15. **注意力方法和 Neck 替换的边际收益递减**: T11 (FECA, +5 params/block)、T12 (C3K3, +3.5M params)、T14 (ASFF) 均未显著超越 T5 baseline。T15 (ASFF+FECA 组合) 甚至倒退。说明在 YOLO11l+Fusion 的强基线上, 注意力调整和 Neck 替换的增益已经饱和。
15. **损失函数替换全部失败**: T8 (Shape-IoU)、T9 (WIoU)、T10 (NWD) 全部未超越默认 CIoU。CIoU 在 NSLSR-LWIR 数据集上表现稳健, 替换损失函数带来的不确定性大于收益。
16. **SAHI 滑窗推理对泛化良好模型无效, 对过拟合模型有诊断价值**: T28 系统评估显示 SAHI 在 10 个模型上均未超越标准推理 (mAP50 下降 10-62%)。唯一例外是严重过拟合的 T1/T2 (标准 mAP50=37.67%), SAHI 将 Recall 从 39% 恢复至 92%。SAHI 不适合作为标准推理组件, 但可作为过拟合诊断工具。
17. **P2 小目标检测头在 16GB VRAM 限制下不可行**: P2 (160×160) 检测头的特征图面积是 P5 的 64 倍, YOLO11m+Fusion+P2 总显存需求 ~18-22GB。bath=2 训练速度 >8s/step, 无法在合理时间内完成。已取消。替代方案 CRRP 增强 (T16) 已达 98.13% mAP50, 远优于 P2 可能带来的边际收益。

---

*本实验记录与 `yolo-training/docs/training_record.md` 互补 — 该文件包含更详细的每 Run 分析、融合架构图和下一步修改建议。*
